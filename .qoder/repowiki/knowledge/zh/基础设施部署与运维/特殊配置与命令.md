- **构建流程**：依次执行 `scripts/build-jars.sh`（Maven 多模块打包）和 `scripts/build-admin-ui.sh`（前端构建）。
- **环境初始化**：首次运行需复制 `deploy/.env.example` 为 `deploy/.env` 以配置端口、数据库密码及 JVM 参数。
- **启停命令**：通过 `scripts/docker-up.sh` 一键拉起全栈服务，`scripts/docker-down.sh` 停止并清理容器。