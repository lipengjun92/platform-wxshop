- 采用分层架构描述：访问入口层（Admin UI/小程序/Uni-app）、应用服务层（Admin/API双后端）、代码模块层（Biz/Common/XML）、数据基础设施层（MySQL/Redis）及外部集成层。
- 明确模块依赖方向：Admin与API服务均依赖Biz核心业务模块，Biz依赖Common公共模块，数据访问以MyBatis XML驱动为主。
- 定义双链路调用模型：后台管理链路（UI->Admin->Biz->DB）与商城用户链路（Client->API->Biz->DB/Third-party）。
- 包含关键业务时序图：覆盖微信登录、购物车结算、下单、微信支付回调、后台发货及确认收货的全流程交互。