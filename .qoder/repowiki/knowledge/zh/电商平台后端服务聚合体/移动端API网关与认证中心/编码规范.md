- 所有业务控制器继承自`AppBaseController`以复用获取客户端IP、小程序AppId及自动Trim字符串等基础功能。
- 接口入参统一封装为以`Api`或`Login`为前缀的Request对象（如`ApiCartAddRequest`），出参使用Vo或通用Result包装。
- 敏感或需登录的接口通过移除`@IgnoreAuth`注解或配置拦截器排除路径来强制进行JWT令牌校验。