- **依赖层级**：采用严格的单向依赖链，`platform-common`为基础工具层，`platform-biz`依赖common封装核心业务，`platform-admin`与`platform-api`作为应用入口层均依赖biz。
- **启动隔离**：管理后台（Admin）与移动端接口（API）分别由独立的Spring Boot主类驱动，支持针对不同端口和上下文路径进行差异化配置与独立部署。
- **统一治理**：父POM集中定义Spring Boot、MyBatis-Plus、Shiro等全栈技术版本，确保各子模块在数据源、安全认证及ORM框架上的行为一致性。