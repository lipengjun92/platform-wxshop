- 所有云存储实现类均继承自 `AbstractCloudStorageService` 并重载 `upload` 系列方法以屏蔽底层差异。
- 实体类 `SysOssEntity` 与配置类 `CloudStorageConfig` 均使用 Lombok `@Data` 注解简化样板代码。
- 服务实现类 `SysOssServiceImpl` 继承 MyBatis-Plus 的 `ServiceImpl` 以获得基础 CRUD 能力。