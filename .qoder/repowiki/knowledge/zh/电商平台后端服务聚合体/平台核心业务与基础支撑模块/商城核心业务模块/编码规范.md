- 所有 Service 接口继承 `IService<T>`，实现类继承 `ServiceImpl<Dao, Entity>` 并标注 `@Service`。
- 所有 Dao 接口继承 `BaseMapper<T>` 并标注 `@Mapper`，基础 CRUD 由框架提供，复杂查询在 XML 中定义。
- 实体类统一使用 `@TableName` 映射表名，`@TableId` 定义主键策略，非表字段使用 `@TableField(exist = false)` 标识。
- 批量修改场景（如商品规格、相册）采用“先删后增”的事务性替换策略，确保子表数据与主表状态同步。