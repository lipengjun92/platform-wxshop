- 采用经典的分层架构：`entity` 定义数据模型，`dao` 配合 `resources/mapper` XML 实现 MyBatis-Plus 数据访问，`service` 封装业务逻辑。
- 引入聚合根模式：通过 `MallGoodsAggregateService` 统一管理商品主信息、规格、属性、相册及 SKU 的复杂关联操作，确保数据一致性。
- 依赖方向明确：Service 层依赖 Dao 层，Dao 层映射至数据库表，支持动态 SQL 查询与分页处理。