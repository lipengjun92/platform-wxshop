- 所有子模块的 Service 接口与实现类均放置在 `service` 与 `service/impl` 目录下，保持命名一致性。
- 通用校验逻辑通过 `common.validator` 包下的分组接口（如 `AddGroup`, `UpdateGroup`）结合 `ValidatorUtils` 实现。
- 涉及并发安全或流量控制的业务方法，通过自定义注解（`@ServiceLock`, `@ServiceLimit`）配合 AOP 切面进行声明式增强。