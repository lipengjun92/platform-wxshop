- 采用标准的 DAO-Service 分层架构，DAO 层基于 MyBatis-Plus 的 BaseMapper 实现基础 CRUD，并通过 XML 映射文件处理复杂查询。
- Service 层继承 IService 接口，实现类通过 @RequiredArgsConstructor 注入依赖，部分服务（如 SysConfigService）集成 Redis 缓存以提升配置读取性能。
- 模块内包含 ShiroService 专门处理权限校验与 Token 管理，以及 MailService 封装邮件发送逻辑，体现了功能内聚的设计原则。