- 路由跳转统一使用`name`属性而非`path`，以增强路由重构时的健壮性。
- 视图组件命名遵循`[module]-add-or-update.vue`模式，将新增与编辑逻辑合并在同一组件中处理。
- 全局工具方法（如权限校验`isAuth`、数据翻译`transDict`）通过`Vue.prototype`挂载，实现跨组件便捷调用。
- API请求默认开启全局Loading遮罩，可通过配置项`loading: false`或`showError: false`在特定场景下关闭。