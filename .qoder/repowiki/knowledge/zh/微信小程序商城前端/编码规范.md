- 页面组件遵循微信标准四件套命名规范：`.js` (逻辑), `.json` (配置), `.wxml` (结构), `.wxss` (样式)。
- 网络请求统一通过 `utils/request` 发起，自动在Header中注入 `token`，并对401状态码进行全局拦截跳转至授权页。
- 用户登录态通过 `wx.setStorageSync` 本地存储 `userInfo` 和 `token`，并在 `services/user.js` 中封装了微信登录与后端会话校验的组合逻辑。