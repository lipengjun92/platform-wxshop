- API 接口地址统一在 `utils/api.js` 中通过常量映射管理，业务逻辑中引用常量而非硬编码 URL。
- 页面样式遵循 uni-app 规范，全局公共样式定义在 `common/app.css` 并在 `App.vue` 中引入。
- AI 技能模块采用标准化结构，包含 `apis`（接口逻辑）、`components`（UI 卡片）及 `SKILL.md`（描述文档），并通过 `index.js` 注册到小程序上下文。