<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.platform.modules.mall.dao.MallCommentPictureDao">

    <sql id="selectSql">
        SELECT
        T.ID,
        T.COMMENT_ID,
        T.PIC_URL,
        T.SORT_ORDER
        FROM MALL_COMMENT_PICTURE T
        WHERE 1=1
        <if test="params.id != null and params.id.trim() != ''">
            AND T.ID = #{params.id}
        </if>
        <if test="params.commentId != null and params.commentId.trim() != ''">
            AND T.COMMENT_ID = #{params.commentId}
        </if>
    </sql>

    <select id="selectMallCommentPicturePage" resultType="com.platform.modules.mall.entity.MallCommentPictureEntity">
        <include refid="selectSql"/>
    </select>

    <select id="queryAll" resultType="com.platform.modules.mall.entity.MallCommentPictureEntity">
        <include refid="selectSql"/>
    </select>

    <select id="queryList" resultType="com.platform.modules.mall.entity.MallCommentPictureEntity">
        select * from mall_comment_picture
        where 1 = 1
        <if test="commentId != null">
            and comment_id = #{commentId}
        </if>
        <choose>
            <when test="sidx != null and sidx.trim() != ''">
                order by ${sidx} ${order}
            </when>
            <otherwise>
                order by id desc
            </otherwise>
        </choose>
        <if test="offset != null and limit != null">
            limit #{offset}, #{limit}
        </if>
    </select>
</mapper>
